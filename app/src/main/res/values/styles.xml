<resources>

    <!-- A single-Activity Compose app only needs a bare platform window theme;
         all real theming (colors, typography, shapes) comes from FeelingsGuideTheme in Compose. -->
    <style name="Theme.FeelingsGuide" parent="android:Theme.DeviceDefault.NoActionBar" />

</resources>
