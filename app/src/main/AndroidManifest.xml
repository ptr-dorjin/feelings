<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="feelings.helper"
    android:installLocation="internalOnly">

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".FeelingsApplication"
        >

        <meta-data android:name="com.google.android.gms.games.APP_ID"
            android:value="123456789012" />

        <activity android:name=".ui.questions.QuestionsActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.schedule.ScheduleActivity"
            android:parentActivityName=".ui.questions.QuestionsActivity"
            android:label="@string/schedule_activity_title">
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.questions.QuestionsActivity" />
        </activity>

        <activity android:name=".ui.answers.AnswerActivity"
            android:parentActivityName=".ui.questions.QuestionsActivity"
            android:label="@string/answer_activity_title">
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.questions.QuestionsActivity" />
        </activity>

        <receiver android:name=".alarm.AlarmReceiver" />

        <receiver android:name=".alarm.BootReceiver"
            android:label="FeelingsHelperOnBootAlarmRefresher">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
        </receiver>
    </application>

</manifest>